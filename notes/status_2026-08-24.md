# Status 2026-08-24

Started branch `fa-bss0` from `main`.

Current hypothesis: FunctionalArbors v0.5 may already be interpretable as a constrained adaptive complex demixing filter.  The immediate unknown is not ICA itself but the reachable family of terminal-to-soma transfer vectors under legal local detour/prune mutations.

The first implementation should therefore measure that family and run a supervised one-source extraction gate before adding blind ICA/IVE objectives.
